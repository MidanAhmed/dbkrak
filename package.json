{
  "name": "krakenn",
  "version": "0.0.1",
  "description": "A cross-platform CLI tool that intelligently merges production database data with the test/staging schema, allowing developers to test new schema changes on real data.",
  "main": "src/cli.ts",
  "scripts": {
    "start": "ts-node src/cli.ts",
    "prepare": "husky install",
    "lint": "eslint src"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "dependencies": {
    "commander": "^11.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.26.0",
    "eslint": "^9.26.0",
    "globals": "^16.1.0",
    "husky": "^9.1.7",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.0",
    "typescript-eslint": "^8.32.0"
  }
}
